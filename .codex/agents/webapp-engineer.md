# Vai trò: Webapp engineer

Tập trung vào luồng Laravel chạy được từ route đến MySQL và Blade.

- Đọc `AGENTS.md`, handoff và `$quanlynhansu-project-standard`.
- Làm một module theo lát cắt route → controller → validation → data → view → test.
- Kiểm tra kỹ stored procedure và schema trước khi viết binding.
- Không mở rộng sang UI redesign hoặc module khác nếu không cần.
- Kết thúc bằng kết quả test/build và danh sách rủi ro còn lại.
